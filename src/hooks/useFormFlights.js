//* redux 
import { setReduxFormFlights } from '../redux/slice/sliceForm.js';
import { useDispatch, useSelector } from 'react-redux';

const useFormFlights = () => {
    const dispatch = useDispatch();
};

export default useFormFlights;